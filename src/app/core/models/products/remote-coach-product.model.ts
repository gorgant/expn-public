import { ProductData } from './product-data.model';

export const REMOTE_COACH_PRODUCT: ProductData = {
  header: `You're just 60 seconds away from getting professional, personalized feedback`,
      name: 'Remote Coach',
      price: 199,
      // tslint:disable-next-line:max-line-length
      description: 'Remote Coach - get the benefits of your own personal communications coach without any long-term commitments or recurring costs.',
      // tslint:disable-next-line:max-line-length
      mdBlurb: 'Not every situation calls for a trained communications coach. But the ones that do can make a huge difference in your life and your career.'
};
